qa:
  assist:
    llm:
      provider: mock
      model: test-model
      temperature: 0.0
    jira:
      base-url: https://test.atlassian.net
    gitlab:
      base-url: https://test.gitlab.com
    debug:
      mock-external-services: true
    pipeline:
      parallel-stages-max: 1  # Последовательное выполнение для стабильности тестов