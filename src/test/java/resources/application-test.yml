# src/test/resources/application-test.yml
spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///qa_assist_test
  jpa:
    hibernate:
      ddl-auto: create-drop

qa-assist:
  llm:
    provider: test
    model: test-model
  jira:
    base-url: https://test.atlassian.net
    api-token: test-token
    email: test@example.com
  pipeline:
    auto-mode: false